spring.application.name=MicroServiceAlmacen

server.port=8081

# ==========================
# BASE DE DATOS POSTGRESQL (Docker o local)
# ==========================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================
# JPA / HIBERNATE
# ==========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ==========================
# JWT CONFIGURACIÓN
# ==========================
jwt.secret=p3u9FhZ@4!aB6nT$kX7rVq2^jP0zYwL&dH8sJm#QfR1tN*Zc5oGxE%lK9vU0bI


# ==========================
# LOGGING / DEBUG
# ==========================
logging.level.org.springframework.data.jpa=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.web.client.RestTemplate=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.root=INFO

# Logs de Hibernate
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Logs de errores
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework=DEBUG
# ==========================
# MENSAJES DE VALIDACIÓN
# ==========================
spring.messages.basename=messages
# application.properties
logging.level.com.ApiarioSamano.MicroServiceAlmacen=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n


# ==========================================
# CONFIGURACIÓN CORS
# ==========================================
cors.allowed-origins=http://localhost:4200

# ==========================================
# Rutas microservices
# ==========================================
microservicio.generador.base-url=${MICROSERVICIO_CODIGOS_URL}
microservice.proveedores.url=${MICROSERVICIO_PROVEEDORES}
microservicio.produccion.base-url=${MICROSERVICIO_PRODUCCION}